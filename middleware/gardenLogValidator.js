const gardenLogValidator=require("../helpers/validate");function saveEntry(req,res,next){const validationRule={year:"required|numeric",seedName:"required|string",seedType:"string",variety:"required|string",plantedQty:"required|string",plantedDate:"required|string",harvestedQty:"string",harvestedDate:"string",notes:"string"};gardenLogValidator(req.body,validationRule,{},(err,status)=>{status?next():res.status(412).send({success:!1,message:"Validation failed",data:err})})}module.exports={saveEntry:saveEntry};