const router=require("express").Router(),seedsController=require("../controllers/seeds"),validation=require("../middleware/seedValidator");router.get("/",seedsController.getAllSeeds),router.get("/:id",seedsController.getOneSeed),router.post("/",validation.saveSeed,seedsController.createSeed),router.put("/:id",validation.saveSeed,seedsController.updateSeed),router.delete("/:id",seedsController.deleteSeed),module.exports=router;