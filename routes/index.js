const router=require("express").Router(),{ensureAuth:ensureAuth,ensureGuest:ensureGuest}=require("../middleware/auth");router.get("/",ensureGuest,(req,res)=>{let html="<h1>Garden API Login</h1>";html+="<p>Log in with your Google Account to see the api-docs.</p>",html+="<button onclick=\"window.location.href='/auth/google';\">Log In</button>",res.send(html)}),router.use("/api-docs",ensureAuth,require("./swagger")),router.use("/seeds",ensureAuth,require("./seeds")),router.use("/garden-log",ensureAuth,require("./garden-log")),router.use("/auth",require("./auth")),module.exports=router;